[project]
name = "InterviewPrep"
version = "1.0"
description = "Preparation for interview"
requires-python = ">=3.10"

[project.scripts]
prep="preparation.preparation:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"